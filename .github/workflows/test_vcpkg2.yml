name: test_vcpkg2
permissions:
  contents: write

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'test'
        required: false
        default: 'demo'

jobs:
  build-deps:
    runs-on: windows-latest
    strategy:
      matrix:
        myarch: [x86-windows, x64-windows]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install vcpkg
        shell: cmd
        run: |
          git clone https://github.com/microsoft/vcpkg.git
          cd vcpkg
          git checkout 2025.07.25
          cd ..
          call .\vcpkg\bootstrap-vcpkg.bat

      - name: Install deps for ${{ matrix.myarch }}
        shell: cmd
        run: |
          set INSROOT=install-root-${{ matrix.myarch }}
          .\vcpkg\vcpkg.exe install --triplet ${{ matrix.myarch }} --x-install-root=%INSROOT%

      - name: Export ZIP for ${{ matrix.myarch }}
        shell: cmd
        run: |
          set INSROOT=install-root-${{ matrix.myarch }}
          .\vcpkg\vcpkg.exe export --zip --raw --output-dir=vcpkg-deps-${{ matrix.myarch }} --x-install-root=%INSROOT%

      - uses: actions/upload-artifact@v4
        with:
          name: vcpkg-deps-${{ matrix.myarch }}
          path: vcpkg-deps-${{ matrix.myarch }}
